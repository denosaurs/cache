name: CI check

on:
  push:
    branches: [ master, dev ]
  pull_request:
    branches: [ master, dev ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout sources
      uses: actions/checkout@v2
      
    - name: Setup latest deno version
      uses: denolib/setup-deno@v2
      with:
        deno-version: v1.x

    - name: Check deno fmt
      run: deno fmt --check
    
    - name: Check deno lint
      run: deno lint --unstable
      
  test:
    name: ${{ matrix.kind }} ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      matrix:
        os: [macOS-latest, windows-latest, ubuntu-latest]
    steps:
    - name: Checkout sources
      uses: actions/checkout@v2
      
    - name: Setup latest deno version
      uses: denolib/setup-deno@v2
      with:
        deno-version: v1.x

    - name: Run deno test
      run: deno test --allow-read --allow-write --allow-net --unstable
